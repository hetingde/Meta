#.github\workflows\schedule.yml
on:
  schedule:
    - cron: "10 */8 * * *"
  workflow_dispatch:
name: Excavator
jobs:
  excavate:
    name: Excavator
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@main
      - name: Excavator
        uses: ScoopInstaller/Scoop-GithubActions@main
        env:
          USER_EMAIL: zxj@outlook.sg
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          SKIP_UPDATED: "1"
